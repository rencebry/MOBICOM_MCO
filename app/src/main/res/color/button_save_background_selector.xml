<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the button is enabled -->
    <item android:state_enabled="true" android:color="@color/vinyl_mint" />
    <!-- Default state (when the button is disabled) -->
    <item android:color="@color/timer_background_gray" />
</selector>